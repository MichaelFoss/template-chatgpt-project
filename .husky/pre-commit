
yarn lint-staged

if ! git diff --quiet -- sources || [ -n "$(git ls-files --others --exclude-standard -- sources)" ]; then
  echo "Skipping full source validation because sources/ has unstaged changes or untracked files."
  echo "Run 'yarn validate:sources' manually when source drafts are ready."
  exit 0
fi

yarn validate:sources
