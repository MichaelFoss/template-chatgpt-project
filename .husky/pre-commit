
yarn lint-staged

if [ -n "$(git status --porcelain -- sources)" ]; then
  echo "Skipping full source validation because sources/ has unstaged or untracked changes."
  echo "Run 'yarn validate:sources' manually when source drafts are ready."
  exit 0
fi

yarn validate:sources
